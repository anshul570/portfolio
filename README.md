# Portfolio Setup

This is a local-first portfolio starter for Anshul Shrivastava.

Files:

- `index.html`: Main portfolio page
- `styles.css`: Visual system and layout
- `content.js`: Structured source content used by the page and chatbot
- `app.js`: Rendering logic and lightweight local chatbot behavior

Why this setup:

- Opens directly as a local file without a dev server
- Keeps the portfolio easy to edit while content is still evolving
- Leaves a clean path to replace the local chatbot logic with real retrieval later

Next content upgrades:

1. Add GitHub projects and links once the exact repos to feature are chosen
2. Replace the case study placeholder with the churn writeup
3. Add personal notes, hobbies, and longer-form background to the chatbot knowledge
4. Swap the local answer engine for real RAG when the content base is ready
